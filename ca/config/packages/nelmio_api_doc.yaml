nelmio_api_doc:
    documentation:
        host: 172.18.0.7
        schemes: [http, https]
        info:
            title: CA
            description: It's a microservice that manages membership CA
            version: 1.0.0
        # securityDefinitions:
        #     Bearer:
        #         type: apiKey
        #         description: 'Value: Bearer {jwt}'
        #         name: Authorization
        #         in: header
        # security:
        #     - Bearer: []            
    areas: # to filter documented areas
        path_patterns:
            - ^/(?!/doc$) # Accepts routes under / except /api/doc